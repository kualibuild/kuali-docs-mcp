{
  "dxt_version": "0.1",
  "name": "kuali-docs-mcp",
  "display_name": "Kuali Docs",
  "version": "1.0.0",
  "description": "Create and review Google Docs for Kuali PRDs and specs. Works with the shared Kuali docs folder.",
  "author": {
    "name": "Kuali"
  },
  "server": {
    "type": "python",
    "entry_point": "kuali_docs_mcp.server:main",
    "mcp_config": {
      "command": "uvx",
      "args": ["kuali-docs-mcp"],
      "env": {
        "GOOGLE_SERVICE_ACCOUNT_KEY": "${user_config.service_account_key}",
        "DRIVE_FOLDER_ID": "${user_config.drive_folder_id}"
      }
    }
  },
  "user_config": {
    "service_account_key": {
      "type": "string",
      "title": "Service Account Key (JSON)",
      "description": "Paste the contents of the Kuali service account JSON key. Ask your team lead for this.",
      "secret": true,
      "required": true
    },
    "drive_folder_id": {
      "type": "string",
      "title": "Google Drive Folder ID",
      "description": "The ID of the shared Kuali docs folder in Google Drive.",
      "default": "0ADdItM1HkFgXUk9PVA",
      "required": true
    }
  }
}
